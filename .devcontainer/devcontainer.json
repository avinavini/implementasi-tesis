{
  "name": "implementasi-tesis",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt && python -c \"import nltk; [nltk.download(p, quiet=True) for p in ('stopwords', 'punkt', 'punkt_tab')]\"",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  },

  "forwardPorts": [8501],
  "portsAttributes": {
    "8501": {
      "label": "Streamlit dashboard",
      "onAutoForward": "notify"
    }
  }
}
